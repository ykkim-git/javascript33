/**
 * DOM (Document Object Model)
 * HTML과 CSS의 구조와 스타일을 파싱하기 위해서 브라우저는 
 * document의 겉모양을 만들고 이 모델은 js가 object로써의 웹사이트 document의
 * 컨텐츠와 엘리먼트에 접근할 수 있도록 한다.
 */

/**
 * DOM 접근방법?
 * 스크립트를 작성할 때 문서 자체를 조작하거나 문서의 자식요소를 얻기위해
 * document또는 window elements를 위한 API를 즉시 사용할 수 있다.
 */

 /**
  * DOM의 핵심 interfaces
  document.getElementById(id)
  document.getElementsByTagName(name)
  document.createElement(name)
  parentNode.appendChild(node)
  element.innerHTML
  element.style.left
  element.setAttribute
  element.getAttribute
  element.addEventListener
  window.content
  window.onload
  window.dump
  window.scrollTo
 */